---
name: implement-feature
description: Drive a change end to end - ticket, branch, implementation in small steps, the gates, commit, request and the review loop. Use when asked to "implement", "build a feature", "work on issue #N", "fix a bug", "start on the ticket", or "pick up" a piece of work.
---

# implement-feature

This repository is the devkit itself, so the skill is not vendored here. The
authoritative instructions are in `project/core/skills/implement-feature/SKILL.md` — read that file and
follow it.

Where it names a file at the repository root, read it from its pack instead:

| It says | Here it is |
|---|---|
| `AGENTS.core.md` | `project/core/AGENTS.core.md` |
| `AGENTS.<forge>.md` | `project/forges/github/AGENTS.github.md` — this repository is on GitHub |

There are no stack packs here, so `DEVKIT_STACKS` is empty and no stack rules
apply. The helper scripts next to this file are shims onto the same source
directory; call them by the paths the instructions give.

Generated by `.devkit/stubs.sh` — do not edit.
