# AGENTS.github.md

<!-- devkit: PLACEHOLDER. Content lands in step 2, extracted from CashPrism. -->

**Placeholder — no rules in force yet.**

This file will carry the GitHub-shaped conventions: branch naming, conventional
commits, the issue schema, the pull request body, the labels that carry the
commit type, and the review loop.

Section slugs are prefixed `forge.`, e.g. `{#forge.branches}`, so a repository
that later moves to GitLab keeps its deviations pointing at the same rules.

Managed file — change it in raisr/devkit, not in the repository that received it.
