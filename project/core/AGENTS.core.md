# AGENTS.core.md

<!-- devkit: PLACEHOLDER. Content lands in step 2, extracted from CashPrism. -->

**Placeholder — no rules in force yet.**

This file will carry the rules that hold in every repository, whatever the
stack and whatever the forge: documentation split, English-only, SOLID, the
test layout, the working style, the boundaries, and the signature for content
an agent creates in external systems.

Every section gets a stable slug, so a project can record a deviation that
points at it and `/devkit-sync` knows when the ground it was decided on moved:

    ## Async and cancellation   {#core.async}

Managed file — change it in raisr/devkit, not in the repository that received it.
